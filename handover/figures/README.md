# figures/

| ディレクトリ | 中身 |
|---|---|
| `geometry/` | 形状に関する図。Fig.2 の実測（`fig2_*`）、Fig.7 の数値化（`fig7_digitized`）、生成形状と Fig.2 の重ね（`gamboa_full`）、接合角（`junction_angle`, `loop_junction_angle`）、CAD の一覧（`cad_index`）、変種の一覧（`variants`）、段々流路（`stair*`） |
| `flow/` | 流れの図。速度場・流線（`gamboa_full_fields_*`, `field_Re100`, `fwd_vs_rev`）、Di 対 Re（`di_vs_re`）、周期版の診断（`gamboa_diag_*`） |
| `summary_table.md` | 発表用の数表 |

**どの CAD ファイルがどの形かは `geometry/cad_index.png`**（`cad/INDEX.md` と同じ内容）。
